# HANDOFF — 2026-09-22, RTX 4070 / WSL2 session

Read `CLAUDE.md` first, then this file, then `reports/03-DATA-AUDIT-2026-09-22.md` (what the two new data drops
actually contain), then `reports/02-EXPERIMENTS.md` + `reports/FINAL-REPORT.md` (everything before today).
This file supersedes `tasks/HANDOFF.md` §−1 for anything to do with the current machine.

---

## 0. One-paragraph summary

Two archives arrived (`DataV2`, `dataUpdate`). **DataV2 turned out to contain no new information** — it is the
same corpus the project already audited on 09-19 (0 new images, 629 of its 643 label moves already applied).
**`dataUpdate` is the valuable one**: a re-runnable synthetic Thai-glyph generator, 167 Thai fonts, and 98,071
rendered images, of which 68,071 fall in our 72-class universe and cover exactly the 35 classes that have only
1–155 real images each. Separately, the raw corpus was **missing from this machine** and was reconstructed from
DataV2 with md5 proof. The owner's objective for the remaining work is **generalisation**, not validation
accuracy. Environment is built and verified (RTX 4070, CUDA OK); data prep is done; **no new training has been
run yet** — the experiment configs are written and the pipeline script is ready to fire.

---

## 1. Machine state — read this before running anything

| | |
|---|---|
| GPU | RTX 4070 12 GB, driver 560.94, `torch 2.14.0+cu126`, `cuda.is_available() == True` |
| Training copy (canonical) | **`~/work/thaichar72` inside WSL2 Ubuntu** (ext4, 32 threads, 889 GB free) |
| Windows copy | `D:\Eak_Deeplearn\thaichar72` — holds `.git`; docs were authored here and pushed into WSL |
| Extracted drops | `~/work/incoming/datav2/` (images) and `~/work/incoming/synth/dataset/` (dataUpdate) |
| Original zips | `D:\Eak_Deeplearn\*.zip`, extracted copies under `D:\Eak_Deeplearn\incoming\` |
| `agy` | **not installed** (only the Antigravity IDE binary is on PATH). Owner's standing approval applies: write code yourself, log `SELF` rows in `tasks/DELEGATION-LOG.md` |

### 1.1 CRITICAL — `uv sync` does not work on this machine

Two independent problems, both already worked around. **Do not run a bare `uv sync` or `uv run`.**

1. uv's *managed* Python cannot be inspected on this box:
   `failed to query metadata … The path cannot be traversed because it contains an untrusted mount point (os error 448)`.
   Fixed by pinning the interpreter: `uv venv --python C:/Users/PC02/.pyenv/pyenv-win/versions/3.12.10/python.exe`
   (Windows side). In WSL the system `python3` is fine.
2. uv's HTTP client **wedges against `download.pytorch.org`** — it sits at 0 B/s indefinitely while
   `curl` pulls the identical wheel at 15 MB/s. It stalled three times at exactly `Downloading torch (828.9MiB)`.
   PyPI itself is fine (5.5 GB of `nvidia-*` wheels downloaded normally).

The venv was therefore built manually:

```bash
cd ~/work/thaichar72
mkdir -p ~/wheels && cd ~/wheels
curl -L -O "https://download.pytorch.org/whl/cu126/torch-2.14.0%2Bcu126-cp312-cp312-manylinux_2_28_x86_64.whl"
curl -L -O "https://download.pytorch.org/whl/cu126/torchvision-0.29.0%2Bcu126-cp312-cp312-manylinux_2_28_x86_64.whl"
cd ~/work/thaichar72
uv export --format requirements-txt --no-hashes --no-emit-project -o /tmp/reqs.txt
uv pip install --find-links ~/wheels -r /tmp/reqs.txt
uv pip install -e . --no-deps
```

**Consequence: always use `uv run --no-sync python …` (or call `.venv/bin/python` directly).** A plain `uv run`
re-syncs to the lock and would undo the manual install. `tasks/run_gen_pipeline.sh` already does this.

---

## 2. The data situation

### 2.1 The corpus was missing and has been reconstructed (md5-proved)

`ThaiCharacter Dataset/` and all of `data/` were absent on this machine. `reports/eda/files.csv` (tracked, 62,707
rows with per-file md5) plus DataV2's byte-identical images made reconstruction possible:

```
uv run --no-sync python scripts/restore_round2_from_datav2.py \
    --datav2 ~/work/incoming/datav2 --out "ThaiCharacter Dataset/round2" --link
→ restored 62,522 | md5 verified 62,522 | mismatch 0 | missing 185
```

`scripts/restore_round2_from_datav2.py` places each file back under the code in `files.csv` (i.e. the **original**
labels) and skips every `aug_` file. Label corrections continue to live only in
`reports/analysis/datav2_label_changes.csv`, applied by `scripts/make_split_v2.py`. Report:
`reports/analysis/round2_restore.json`.

### 2.2 The split is 175 rows smaller than the historical one — this matters

The 185 files DataV2 deleted are gone for good; 175 of them survived our own cleaning rules, so:

| | historical | now |
|---|---:|---:|
| clean index | 60,117 | **59,942** |
| stratified val | 12,027 | 11,993 |

`scripts/prep_data.py` now filters non-existent paths explicitly and writes
`reports/analysis/absent_from_restore.csv`. Because the stratified split is a deterministic per-class shuffle,
removing those rows **shifts split membership**, so today's numbers are *not* bit-comparable with the 93
historical runs. Evidence — the shipped K1 weight re-evaluated on the regenerated v2 split:

| metric | historical | on regenerated split |
|---|---:|---:|
| top-1 | 0.9903 | **0.9910** |
| balanced | 0.9875 | **0.9943** |
| minority acc | — | **1.0000** |

Close enough to prove the pipeline is intact, but **mildly optimistic** — a few images K1 trained on have landed
in val, which inflates the tail most (hence balanced +0.7 pt). **Therefore: never compare a new run against a
historical number. `tasks/run_gen_pipeline.sh stage2` re-runs the F19 recipe as `F19_ctrl_v2_4070` on this exact
split, and that control is the only legitimate baseline for the new work.**

### 2.3 What the two drops actually are

Full analysis with all counts: `reports/03-DATA-AUDIT-2026-09-22.md`. Headlines:

- **DataV2** — 0 new images; 643 label moves of which 629 already applied; 14 outstanding (9 after the "Copy of"
  rule, all in the า/ว/จ/ใ cluster); its 1,822 `aug_` images are **rejected** (augmented pre-split → would leak
  augmented copies of training images into validation). New artefact worth using: `swap_verdict.csv`, a human QA
  of 13 confusable pairs flagging **6 still contaminated** (ต/ด, า/จ, ๅ/ว, า/ใ, ต/ค, ว/ร).
- **dataUpdate** — generator + 167 fonts + 98,071 images. Only **35 of its 50 classes (68,071 images)** are in
  our universe; the other 15 (ฅ ฆ ฌ ฎ ฦ ะ ำ ฺ ฿ ๋ ํ ๎ ๏ ๚ ๛) are not part of the assignment and are kept
  separately as possible extra pretraining classes.

### 2.4 The finding that should drive the remaining work

The 35 classes `dataUpdate` covers hold ~2,100 real images = **3.4 % of the corpus but 49 % of the class list**.
Their validation sets are **1–31 images**; ฃ and ฑ have **n_val = 0, n_train = 1**. So the headline 99 % is
dominated by the head, and half the class list is effectively untested. If the hidden test is anywhere near
class-balanced, that is where the marks are.

---

## 3. What is already built and ready to run

Everything below is committed on the Windows copy and synced into `~/work/thaichar72`.

**New scripts**
- `scripts/restore_round2_from_datav2.py` — corpus reconstruction (done, see §2.1)
- `scripts/prep_dataupdate.py` — imports dataUpdate into the engine's extra-data format. Emits four pairs:
  `index.csv` (in-universe **train only**), `index_probe.csv` (in-universe **val**, held out — see below),
  `index_oov.csv` (the 15 out-of-universe classes), each with a matching `glyphs*.npz`.
- `scripts/merge_extra_sources.py` — concatenates several `(index.csv, glyphs.npz)` pairs into one, since the
  engine accepts only a single `extra_train_*`.
- `tasks/run_gen_pipeline.sh` — stages `prep | verify | synth | extra | stage1 | stage2`.

**Held-out probe (important for honest evaluation).** dataUpdate ships its own train/val split. Only its `train`
side is ever trained on; its `val` side becomes an untouched probe for the 35 starved classes, whose real val sets
are too small to mean anything. It is synthetic, so it is a **domain proxy reported separately and never used to
select a model**.

**Font pool.** `data/fonts_all/` = **203 typefaces** (26 repo + 142 new from dataUpdate + 35 Windows Thai print
faces: THSarabun, Angsana, Browallia, Cordia, the UPC family, Leelawadee — the faces scanned Thai documents are
actually set in), vs 26 for the incumbent B6a init. It lives under `data/` **deliberately**, because `/data/` is
gitignored and the Windows faces must not be redistributed. 3 `.ttc` collections are skipped (`synth.py` globs
`*.ttf` only) — picking those up is a possible small win.

**Configs** (`configs/gen/`):

| config | what it tests |
|---|---|
| `G1_synth203_pretrain_r18_64` | stage-1 on the 203-font render, all 72 classes |
| `G2_synth203du_pretrain_r18_64` | stage-1 on 203-font render **+ dataUpdate** |
| `G3_r18_g1init_randaug_20` | F19 recipe, initialised from G1 |
| `G4_r18_g2init_randaug_20` | F19 recipe, initialised from G2 |
| `G5_r18_g2init_randaug_fill500_20` | G4 **+ tail-fill to 500/class during fine-tuning** (`extra_mode: fill`) |

The design follows the project's own established result that synthetic works better as *pretraining* than as
extra data (B6a_ft 97.92/98.43 > B6_synth_all 98.04); G5 tests the middle ground.

---

## 4. Status of each stage

| stage | state |
|---|---|
| `prep` | ✅ done — 59,942 clean rows, splits seeds 42/0/1 + `split_seed42_v2.csv` (629 moves replayed), cache 34.2 MB |
| `verify` | ✅ done — K1 → 0.9910 / 0.9943 (see §2.2 for the caveat) |
| `synth` | ✅ done — 72,000 glyphs in 17.3 s; all 203 fonts pre-audited for `.notdef` (0 dropped) |
| `extra` | ✅ done — 68,071 in-universe; **the shipped probe was 99.2 % contaminated and was rebuilt group-disjointly** |
| `stage1` | ✅ done — G1 0.9395/0.9276, G2 0.9396/**0.9354** (zero-shot synth→real) |
| `stage2` | ✅ done — F19_ctrl / G3 / G4 / G5, then doc-split, gate, probe, 3 seeds, ensemble, KD |

**Everything in §5 below is finished. The study's outcome is in `reports/02-EXPERIMENTS.md` §K and
`reports/FINAL-REPORT.md` §13.** Winner: **G4** (stage-1 on 203 typefaces + dataUpdate, then the F19
fine-tune recipe), shipped as `weights/thaichar72_r18_64_gen.pt`; `configs/final.yaml` now points at it and
the previous recipe is preserved as `configs/final_f19_v2.yaml`.

| | F19_ctrl (baseline, same split) | G4 (shipped) |
|---|---:|---:|
| doc-disjoint top-1 | 0.9856 | **0.9872** |
| robustness gate (≥ 0.8988) | 0.9124 ✅ | **0.9153** ✅ |
| tail probe, unseen writers | 0.2258 | **0.4292** |
| stratified val top-1 | 0.9902 | 0.9902 (saturated) |

### 4.1 What a next agent should know before continuing

1. **The tail probe had to be rebuilt.** dataUpdate's own train/val split is per-image over renders sharing a
   source, so 99.2 % of its val side reused a train source. Use `data/dataupdate/index_probe_dj.csv` +
   `glyphs_probe_dj.npz` (group-disjoint: 5 whole font families, 171 whole writer ids), never `index_probe.csv`.
   `scripts/eval_probe.py` reports the hw and ft halves separately — quote the **hw** number.
2. **The probe is a domain proxy, not a score.** It ranks models that share a fine-tuning recipe; it cannot be
   read as expected hidden-test accuracy, and G5 cannot be compared on it at all (it trained on that domain).
3. **Stratified val is finished as a selection tool** — every candidate sits at 0.9897–0.9902, a spread of ~2 images.
4. **ฃ and ฑ have 1 real training image and 0 validation images each.** No amount of model work makes them
   measurable on real data. If the hidden test is class-balanced this is the largest single risk.
5. Two bugs were fixed in place: `prep_dataupdate.py` (`KeyError: 'family'`) and `merge_extra_sources.py`
   (silently dropped 8,118 rows whose images lived in a non-sibling cache — now warns).
6. **Selective tail-fill was tried and is documented in §L of `reports/02-EXPERIMENTS.md`.** The new
   `extra_fill_max_real` config key fills only classes below a real-image threshold. G6 (n_train≤20 → 200)
   posts the best stratified numbers in the project, but over 3 seeds neither G6 nor G7 beats G4 on anything
   measurable (doc balanced p=0.23) while costing macro-F1 (p=0.085). **G4 stays shipped.** If the teacher
   confirms the hidden test is class-balanced, switch to `configs/gen/G6_r18_g2init_randaug_fill200_max20.yaml`.
7. **The measurement, not the model, is now the bottleneck.** 8 of the 24 starved classes have zero
   document-disjoint validation images and the rest have 2–20, so one image flipping moves 72-class balanced
   accuracy by 0.46 pt. Labelling ~20–30 real images per class for the 14 scarcest classes (~400 images) would
   buy more than any further synthetic generation, because it restores the ability to *measure*.
8. **Never run `uv` under `/mnt/d/...` from WSL.** Doing so deleted and recreated the Windows copy's `.venv`
   as a Linux one (`--no-sync` stops a re-sync, not venv re-creation). Work in `~/work/thaichar72`.
9. Notebook: `notebooks/ThaiChar72_Colab.ipynb` is now 20 cells with a **§5B Error Explorer** (per-sample
   predictions CSV, mistake galleries, per-class recall/precision, zoomed confusion matrix, confidence
   monitoring, `show_class()`), runs clean in JupyterLab and headlessly via `scripts/run_notebook_local.py`.

---

## 5. Exactly what to run next

```bash
cd ~/work/thaichar72

# 1. Render the 203-font synthetic corpus (1000/class x 72 = 72k glyphs).
#    Check the montage in reports/figures/ before trusting it — some Windows faces may
#    render a .notdef box for rarer glyphs; drop any font that does.
bash tasks/run_gen_pipeline.sh synth

# 2. Import dataUpdate + merge the two synthetic sources.
bash tasks/run_gen_pipeline.sh extra

# 3. Stage-1 pretraining, two lanes in parallel (one lane ~35 % GPU — the pipeline is CPU-bound).
bash tasks/run_gen_pipeline.sh stage1

# 4. F19 control + G3/G4/G5, then aggregate.
bash tasks/run_gen_pipeline.sh stage2
```

Then, in order of value:

1. **Doc-disjoint runs of the best two** (`--suffix _doc --set split_kind=doc`) and the **robustness gate**
   (`scripts/robustness.py --ckpt … --binarize`, criterion = mean top-1 over non-clean rows ≥ F2's 0.9038 − 0.005).
   Per `CLAUDE.md`, selection is on the doc split + the gate, **not** the stratified val.
2. **Tail probe**: evaluate each candidate on `data/dataupdate/index_probe.csv` and report per-class recall for
   the 35 starved classes. This is the number that actually answers "did the tail learn anything".
3. 3 seeds of the winner on the same split → mean ± std → ensemble → KD into one model (reuse
   `scripts/ensemble_eval.py`, `configs/final_candidates/K1_r18_kd_20.yaml`).
4. Export weights, rebuild the notebook (`scripts/build_notebook.py` — edit the builder, never the `.ipynb`),
   update `reports/02-EXPERIMENTS.md` + `FINAL-REPORT.md`.

---

## 6. Open decisions and owner's instructions

- **Owner's stated objective (2026-09-22): generalisation.** Prefer recipes that hold up on the doc split, the
  robustness gate and the tail probe over ones that win the stratified val by a tenth of a point.
- **Label noise — owner chose "clean further, keep v1 as a hedge".** Not started. The targets are the 6 pairs
  `swap_verdict.csv` still flags plus K1's actual top confusions (า↔ๅ 30+16, ั↔้ 13+4). Method: find candidates by
  model disagreement, **confirm by eye with a montage before moving anything**, and ship both a cleaned model and
  the v1-label model (`weights/thaichar72_resnet18_64_v1labels.pt`) in case the hidden test follows the original
  convention.
- **Still unanswered by the teacher**: whether the hidden test is class-balanced. This changes how much the tail
  work is worth. Worth asking.
- **Unused ideas**, in rough order of promise:
  - 87-way stage-1 pretraining using the 15 out-of-universe classes as extra negatives, then a 72-way head
    (`index_oov.csv` is already built for this; needs a small change to the head size in stage-1).
  - Font-family-disjoint / looped-vs-loopless generalisation probe: train stage-1 on looped faces only, test on
    loopless. Rigorous generalisation evidence and good report material.
  - From `../katgpt-rs` (owner asked for "outstanding technique"): **Renoise-CE self-verifier** — measure
    prediction *drift* across perturbed views as a confidence signal rather than plain TTA averaging (the project
    already found plain TTA neutral-to-negative); and **Local Branch Routing** — a small specialist head for the
    confusion clusters (า/ๅ, ั/้, ช/ซ). Either would serve the 2 % "interesting idea" mark.
  - Regenerate synthetics with dataUpdate's own generator at different `--hw-ratio` (its README warns 0.30 may be
    too high) and with `--system-fonts`.

## 7. Gotchas that cost time today

- `pkill -f "uv sync"` matches its own command line and kills the calling shell. Use `pkill -f "uv[ ]sync"`.
- Git Bash on Windows mangles heredocs containing apostrophes; author long docs with a file write instead.
- The Windows console is cp874 — set `PYTHONUTF8=1` for anything printing Thai.
- `scripts/eval_split.py` takes `--split-kind {strat,doc}`, not `--split`.
- `runs/*/best.pt` is gitignored, so `configs/final_candidates/F19_*.yaml`'s
  `init_from: runs/B6a_synth_pretrain_resnet18_64/best.pt` does not exist here. Use the packaged
  `weights/thaichar72_r18_synth_pretrain_init.pt` instead (the pipeline's `stage2` already overrides it).
