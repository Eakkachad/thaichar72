# Stage 1 variant: 203-font render PLUS the dataUpdate corpus (68,071 in-universe glyphs,
# 35 tail classes, includes 24.6k real handwriting pushed into the round2 domain).
exp_id: G2_synth203du_pretrain_r18_64
model: resnet18
img_size: 64
use_real_train: false
extra_train_index: data/synth_merged/index.csv
extra_train_cache: data/synth_merged/glyphs.npz
extra_mode: all
aug: full
epochs: 8
batch_size: 256
lr: 0.001
label_smoothing: 0.1
ema: true
