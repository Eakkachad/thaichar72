# CLAUDE.md — operating manual for any agent continuing this project

Project: **72-class Thai character/digit recognition (CNN + Transfer Learning + Data Augmentation)** — a graded
course project (KMITL). Owner: eggchad (67070309@kmitl.ac.th). Language with the owner: **Thai** (code/docs in English
are fine). Read this file first, then `tasks/HANDOFF.md` (exact state + what to do next), then
`reports/02-EXPERIMENTS.md` (all results with interpretation). `tasks/HANDOFF-OWNER-TH.md` is the owner's machine-setup guide.

## 1. Role split (owner's rule — follow it)
- You are the **research lead / planner / reviewer**. Large code chunks are delegated to the Antigravity CLI
  (`agy --dangerously-skip-permissions --model <model> --print "<prompt pointing at tasks/TASK-XX.md>"`) — see
  `tasks/AGY-USAGE.md` and `tasks/DELEGATION-LOG.md`. Owner prefers `gemini-3.8-flash-high`, BUT in `--print` mode
  agy kills any long-running command it started in the background (all models) → use agy only for tasks whose
  commands finish in seconds (writing files, unit tests, plots). Run training/long jobs yourself.
- On the RTX 4060 machine `agy` is probably NOT installed: the owner's standing approval (2026-09-19) applies —
  write the code yourself, log the work as `SELF` rows in `tasks/DELEGATION-LOG.md`, and tell the owner.
- Always: write a spec file `tasks/TASK-XX-<name>.md` before delegating, commit before delegating, run the
  acceptance commands yourself after, log the round in `tasks/DELEGATION-LOG.md`.
- Summarise results to the owner after **every** experiment; be plain about failures.

## 2. Hard constraints
- `ThaiCharacter Dataset/` is READ-ONLY. Never modify/delete. Never commit it (git-ignored).
- Never delete or overwrite existing weight/log files; new runs get new `exp_id`s.
- **No horizontal/vertical flips** in augmentation (Thai glyphs change meaning). Never stretch aspect ratio
  (pad-to-square with margin; `thaichar.transforms.pad_to_square_canvas`).
- Split protocol: dedup first (60,117 images), stratified 80:20 seed 42 (`data/splits/split_seed42.csv`, columns
  `split` and `doc_split`). A model trained on `split_kind=strat` must be evaluated ONLY on `split=="val"`; a
  model trained on `split_kind=doc` only on `doc_split=="val"` — the two partitions overlap.
- Grading: hidden test accuracy (50 % → 1 pt … 100 % → 5 pt) + ranking vs other groups + 2 % for an interesting
  idea. The teacher **stresses generalisation** and may add unusual data → select recipes on the
  document-disjoint split as much as the stratified one; keep Otsu binarisation at inference.
- Reference folders `../katgpt-rs`, `../fly-connectome-template`, `../CNN` (not in this repo) are read-only ideas.

## 3. Repo map
```
src/thaichar/      classes.py (TIS-620 codes ↔ chars), data.py (clean index, splits, GlyphCache, Dataset),
                   transforms.py, augment.py (presets none/base/morph/full/randaug/trivial), synth.py (font renderer),
                   external.py (public datasets), models.py (timm backbones + head), losses.py, samplers.py,
                   metrics.py, engine.py (train_one(cfg) → runs/<exp_id>/), infer.py (load/preprocess/predict)
scripts/           eda.py, prep_data.py, render_synth.py, fetch_external.py, train.py, run_local_queue.py,
                   collect_results.py, ensemble_eval.py, error_analysis.py, plot_results.py, result_figures.py,
                   robustness.py, predict.py, export_weights.py, build_notebook.py, run_notebook_local.py,
                   colab_*.sh (Colab T4 runners — only needed if using Colab again)
configs/           base.yaml (all keys documented), matrix/ (A/B/D/E/S studies), pretrain/ (stage-1/2),
                   final_candidates/ (F1–F17, 20 epochs + TTA)
data/              (git-ignored) cache/glyphs.npz, splits/, synth/, external/  → shipped in the handoff tarball
runs/<exp_id>/     metrics.json + log.csv are tracked in git; best.pt / *.npy are not (tarball carries the needed ones)
reports/           01-EDA-REPORT.md, 02-EXPERIMENTS.md, FINAL-REPORT.md (draft), experiments.{csv,md}, figures/, analysis/
notebooks/         ThaiChar72_Colab.ipynb (generated by scripts/build_notebook.py — edit the builder, not the .ipynb)
tasks/             specs, AGY-USAGE.md, COLAB-USAGE.md, DELEGATION-LOG.md, HANDOFF.md, logs/
weights/           packaged deliverable weights (scripts/export_weights.py)
```

## 4. Environment
- `uv` manages everything; plain `uv sync` / `uv run` is enough. PyTorch wheels are chosen automatically by
  environment markers in `pyproject.toml`: native Windows or WSL2 → CUDA 12.6 wheels (RTX 4060 OK); other Linux → CPU
  wheels. First `uv sync` on WSL2 downloads ≈ 4 GB (torch + nvidia-* + triton; ≈ 3 GB on native Windows) — keep
  the project and `~/.cache/uv` on the same ext4 filesystem (never under `/mnt/c`), ≥ 10 GB free. If
  `torch.__version__` ends in `+cpu` on a GPU box, `uname -r` lacks "microsoft" (WSL1/custom kernel): fix WSL2 rather
  than hand-installing wheels (`uv run` re-syncs to the lock and would undo `uv pip install`; only `uv run --no-sync`
  keeps a manual install). Python 3.12 (`.python-version`). Owner-facing setup guide: `tasks/HANDOFF-OWNER-TH.md`.
- Linux / WSL2 preferred (bash scripts, `fork` workers). Native Windows works for `train.py` /
  `run_local_queue.py` (engine defaults `num_workers=0` on Windows; the augmentation transform is picklable; set
  `PYTHONUTF8=1` for Thai console output) but the `colab_*.sh` scripts do not apply there.
- Tests: `uv run pytest -q` (test_data, test_augment, test_synth, test_engine (~3 min CPU), test_external,
  test_error_analysis, test_infer, test_notebook, test_result_figures).
- Train: `uv run python scripts/train.py --config configs/<x>.yaml [--exp-id NAME] [--set key=value ...]`
  → `runs/<exp_id>/{log.csv,metrics.json,best.pt,last.pt,val_logits.npy,val_labels.npy}`.
  Queue: `uv run python scripts/run_local_queue.py [globs] [--suffix _doc --set split_kind=doc]` (skips done runs).
  Aggregate: `uv run python scripts/collect_results.py` → `reports/experiments.{csv,md}`;
  figures: `scripts/result_figures.py`, `scripts/error_analysis.py --run runs/X`, `scripts/robustness.py --ckpt ...`.
- GPU speed reference: Colab T4 ≈ 28 s/epoch for resnet18@64 on 48k images (20-epoch run + TTA ≈ 10–13 min).

## 5. Git / sync
- Commit small and often with clear messages (owner email above). Results (`runs/*/metrics.json`, `log.csv`,
  `reports/experiments.csv`) are tracked → `git pull`/`push` keeps both machines' result tables merged
  (`collect_results.py` regenerates the table from all metrics.json).
- Weights: only the final packaged `weights/*.pt` (< 100 MB each) go into git; everything else stays local.
