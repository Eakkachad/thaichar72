# data (read-only source, never committed)
ThaiCharacter Dataset/
data/
*.zip
# run artifacts: keep small results (metrics/log) in git, exclude tensors
runs/**/*.pt
runs/**/*.npy
runs/**/*.pth
# weights: only the final packaged files in weights/ are committed (<100 MB each)
*.onnx
*.safetensors
*.ckpt
# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
notebooks/*.executed.ipynb
outputs/
dist/
# misc
.DS_Store
Thumbs.db
*.log
!tasks/DELEGATION-LOG.md
